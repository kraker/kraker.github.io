StylesPath = styles
MinAlertLevel = suggestion

Vocab = Kraker

[*.md]
BasedOnStyles = Vale, Kraker

# Skip Hugo shortcodes and Go template tags so they aren't linted as prose
TokenIgnores = (?sm)({{<[^>]+>}}|{{[^}]+}})
