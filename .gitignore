# Hugo
public/
.hugo_build.lock

# Image optimization backups (manual, via magick)
image-backups/

# Local Claude Code settings
.claude/

# Private brand docs — source of truth lives in GDrive
docs/branding/

# uv / Python (uv.lock IS tracked — pin versions for reproducibility)
.venv/
__pycache__/
*.pyc
.python-version
